[package]
name = "hiwot-supply-chain"
version = "0.1.0"
edition = "2021"

[features]
default = []
test = []

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }