[package]
name = "hiwot-disbursement"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = "=25.1.0"

[dev_dependencies]
soroban-sdk = { version = "=25.1.0", features = ["testutils"] }


